# .bash_profile - sourced for login shells and terminal.app
# Just add .profile (settings added by MacPorts) and .bashrc (all config).

if [ -r ~/.profile ]; then . ~/.profile; fi

if [ -r ~/.bashrc ]; then . ~/.bashrc; fi
